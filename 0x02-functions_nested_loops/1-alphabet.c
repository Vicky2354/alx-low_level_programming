#include "main.h"

/**
 * print_alphabeth - Prints the alphabeth in lowercase
 * 
 * Return: Always 0 (Success)
 */
void print_alphabeth(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i)
	_putchar('\n');
}
